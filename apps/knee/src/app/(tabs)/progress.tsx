// UNREACHABLE in this build: `capabilities.progress` is false — same
// user_progress dependency (and the same removal mechanism) as today.tsx.

export default function Progress() {
  return null;
}
